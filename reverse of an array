#include <bits/stdc++.h>
using namespace std;

int main()
{
  int arr[10],n,i;
  cin>>n;
  for(i=0;i<n;i++)
    cin>>arr[i];
  cout<<"Original array elements are: ";
  for(i=0;i<n;i++)
    cout<<arr[i];
  for(i=n-1;i>=0;i--)        // reverse of an array
    cout<<arr[i];
}
